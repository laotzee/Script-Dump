# About
This script looks for a template in the same directory in which replaces placeholder variables for the parameters the user provides. The absenet placeholders will be ignored.

# Parameters
Inside the template the following placeholders are expected:

- {{company}}
- {{role}}

# Other variables
The script also looks for placeholders that are not required from the user, such as: 

- {{date}}
